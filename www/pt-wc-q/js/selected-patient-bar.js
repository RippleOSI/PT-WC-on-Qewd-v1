export function define_selected_patient_bar(QEWD) {

    //          **** topbar ****

    let component =  {
        componentName: 'ptwq-topheader',
        state: {
            name: 'top-header-patient',
        }
    };

    let hooks = {

    };

    return {component, hooks};
};
